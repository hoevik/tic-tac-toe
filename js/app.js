// A $( document ).ready() block.
$(document).ready(function() {
    console.log( "ready!" );

    $( ".box" ).on( "click", function() {
      var x;
    if ('.box[x]', x%2===0)
        $( this ).css( "color", "red" );
      });
        $( this ).css( "color", "blue" );

    });
